#ifndef ELIXIR_DATA_H
#define ELIXIR_DATA_H

#include <vector>
#include <unordered_set>

namespace elixir {

  using namespace std;

  class Data {
    int fromNodeKey;

    // toNodeKey (children)
    vector<int> toNodeKeys;

  public:
    Data(vector<void *> data)
      : data(data) {

    }

    // data generated by a single node
    vector<void *> data;
  };
}

#endif /* ELIXIR_DATA_H */
